(function(window) {
  //Create div main and appending it to body
  let main = document.createElement('div');
  main.id = 'main';
  main.textContent = "This is main";
  document.body.appendChild(main);

  
})(window);
